<script setup lang="ts">import { computed } from "vue";import type { CorpusRecord } from "../api/pdfCorpus";import type { ProviderProfile } from "../api/system";import { useI18nStore } from "../stores/i18n";import UiButton from "./ui/UiButton.vue";import LlmExecutionControl from "./LlmExecutionControl.vue";const props=defineProps<{record:CorpusRecord;canPrevious:boolean;canNext:boolean;busy?:boolean;profiles?:ProviderProfile[];providerProfileId?:string;modelOverride?:string;concurrencyRisk?:boolean;activeRequests?:number;concurrencyLimit?:number}>();const emit=defineEmits<{(event:"adjudicate",direction:"previous"|"next",providerProfileId:string,model:string):void;(event:"update:providerProfileId",value:string):void;(event:"update:modelOverride",value:string):void}>();const i18n=useI18nStore();const prior=computed(()=>props.record.boundary_llm_before||null);const next=computed(()=>props.record.boundary_llm_after||null);function label(decision?:string){return i18n.t(`pdf_corpus.boundary_llm.${decision||"not_checked"}`)}function confidence(value?:number){return typeof value==="number"?`${Math.round(value*100)}%`:i18n.t("pdf_corpus.confidence_unknown")}</script><template><section class="boundary-adjudication" :aria-label="i18n.t('pdf_corpus.boundary_second_reader')"><header><div><span class="eyebrow">{{i18n.t('pdf_corpus.boundary_second_reader')}}</span><p>{{i18n.t('pdf_corpus.boundary_second_reader_help')}}</p></div></header><LlmExecutionControl v-if="profiles?.length" :model-value="providerProfileId||''" :model-override="modelOverride" :profiles="profiles" :disabled="busy" :task="i18n.t('pdf_corpus.boundary_provider_help')" :concurrency-risk="concurrencyRisk" :active-requests="activeRequests" :concurrency-limit="concurrencyLimit" @update:model-value="value=>emit('update:providerProfileId',value)" @update:model-override="value=>emit('update:modelOverride',value)"/><div class="boundary-grid"><article :class="['boundary-card',{'is-unavailable':!canPrevious}]"><div><b>{{i18n.t('pdf_corpus.previous_boundary')}}</b><span v-if="prior">{{label(prior.decision)}} · {{confidence(prior.confidence)}}</span><span v-else>{{i18n.t('pdf_corpus.boundary_not_checked')}}</span></div><p v-if="prior?.reason">{{prior.reason}}</p><UiButton size="small" variant="soft" :disabled="busy||!canPrevious||Boolean(profiles?.length&&!providerProfileId)" @click="emit('adjudicate','previous',providerProfileId||'',modelOverride||'')">{{i18n.t('pdf_corpus.check_with_llm')}}</UiButton></article><article :class="['boundary-card',{'is-unavailable':!canNext}]"><div><b>{{i18n.t('pdf_corpus.next_boundary')}}</b><span v-if="next">{{label(next.decision)}} · {{confidence(next.confidence)}}</span><span v-else>{{i18n.t('pdf_corpus.boundary_not_checked')}}</span></div><p v-if="next?.reason">{{next.reason}}</p><UiButton size="small" variant="soft" :disabled="busy||!canNext||Boolean(profiles?.length&&!providerProfileId)" @click="emit('adjudicate','next',providerProfileId||'',modelOverride||'')">{{i18n.t('pdf_corpus.check_with_llm')}}</UiButton></article></div></section></template><style scoped>.boundary-adjudication{display:grid;gap:.75rem;padding:1rem;border:1px solid var(--border,#d6d9df);border-radius:12px;background:var(--surface-raised,#fff)}.boundary-adjudication header p{margin:.25rem 0 0;color:var(--text-muted,#59606c);font-size:.875rem;line-height:1.45}.eyebrow{font-size:.75rem;font-weight:700;letter-spacing:.04em;text-transform:uppercase;color:var(--text-muted,#59606c)}.boundary-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:.75rem}.boundary-card{display:grid;align-content:start;gap:.5rem;padding:.75rem;border:1px solid var(--border,#d6d9df);border-radius:10px;background:var(--surface,#fff)}.boundary-card>div{display:flex;justify-content:space-between;gap:.75rem;align-items:baseline;flex-wrap:wrap}.boundary-card span{font-size:.8125rem;color:var(--text-muted,#59606c)}.boundary-card p{margin:0;font-size:.875rem;line-height:1.45}.is-unavailable{border-style:dashed;background:var(--soft)}@media(max-width:760px){.boundary-grid{grid-template-columns:1fr}}</style>