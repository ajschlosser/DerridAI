# 0.61.0 — Undulating Umbrellabird: final status

All planned work is complete on `feature/0.61.0-undulating-umbrellabird`; version identifiers read 0.61.0.

## Completed

- Approved-profile RAG model fix; separate Ruff, mypy, and ESLint CI jobs with tracked debt (`STATIC_ANALYSIS_FOLLOWUPS.md`).
- Backend decomposition (`corpus_metadata.py`, `corpus_pipeline.py`, `corpus_publication.py`, staged enrichment and build orchestration) and frontend lifecycle/review domain extraction, per `REFACTOR_PLAN_0.61.md`.
- Silent-exception audit across `api/app`: correctness-affecting failures surface as build warnings, failed statuses, or propagated errors (`tests/test_0610_failure_visibility.py`); remaining swallows are commented as safe. Last additions: editorial-memory failures are now recorded as build warnings.
- Authentication: environment-driven `SESSION_COOKIE_SECURE`; SQLite-backed per-username failed-login throttle with persistence, expiry, reset, and cross-instance concurrency tests (`tests/test_0610_auth_hardening.py`).
- Documentation: `CHANGELOG.md`, `CONTRIBUTING.md`, `docs/ARCHITECTURE.md`, `docs/notes/0.61.0.md`; README trimmed to the current release. Historical notes already lived one-per-file in `docs/notes/` on master, so the changelog indexes them rather than copying text.

## Validation (Linux, Python 3.12, Node 22)

- `pytest -q`: 149 passed. `ruff check api/app` and `mypy`: clean. `compileall`: clean.
- `npm run lint`, `npm run typecheck`, `npm run test:unit` (34), `npm run build`, `npm run build-storybook`: pass.
- Playwright + axe: 36 passed. Port 6006 was occupied by an unrelated Storybook, so a local uncommitted config on port 16006 with one worker was used. The first run had one cold-start failure (build-readiness story not yet ready within 5 s); that test passed in isolation and the full suite passed with CI's single retry.
- Node 26 breaks Vitest/happy-dom (`localStorage.clear`); CI's Node 22 is the supported runtime.

## Deferred / judgment calls

- Splitting `main.py` into routers and `chroma_store.py` into modules remains a follow-up, as do SA-01–SA-16.
- The login throttle is per username only (no per-IP), so an attacker can lock a known username out for the lockout period; the brief allowed either.
- Fixtures in three older tests still carry `"app_version": "0.60.0"`; they are data, not release declarations, and pass unchanged.
- Docker image builds were not run.
