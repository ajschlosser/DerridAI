# 0.61.0 stopping checkpoint

Implementation is incomplete. Work stopped at the user's request after tested increments.

Completed: approved-profile RAG model fix; separate Ruff/mypy/ESLint gates and tracked analysis debt; metadata-rule extraction; enrichment and build-orchestration decomposition; publication serialization/validation extraction; reactive frontend corpus lifecycle and authoritative review rules with unit tests.

Validation: 475 pytest tests; 34 frontend unit tests; Ruff, mypy, production ESLint, strict vue-tsc. Production and Storybook builds plus 36 Playwright/axe tests passed at baseline; final build/browser reruns are still required.

Remaining, in order:

1. Audit silent exception fallbacks, surface scholarly failures, document safe fallbacks, and add failure-injection regression tests.
2. Add environment-driven secure session cookies and a SQLite-backed failed-login throttle with tests.
3. Split historical release notes into CHANGELOG; add CONTRIBUTING and a code-grounded architecture overview.
4. Update release identifiers (currently still 0.60.0), run all final gates, and review the full diff.

See REFACTOR_PLAN_0.61.md for the approved decomposition and STATIC_ANALYSIS_FOLLOWUPS.md for explicitly deferred analysis debt. Broader main.py and Chroma module splits remain follow-ups. Preserve public interfaces; test and commit each increment, and push checkpoints regularly.
